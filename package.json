{
  "name": "purpld.neocities.org",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rm -rf public",
    "build": "pnpm dlx @11ty/eleventy",
    "serve": "pnpm dlx @11ty/eleventy --serve"
  },
  "packageManager": "pnpm@10.13.1",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "luxon": "^3.7.1"
  }
}